---
"@riseaicloud/ui": patch
---

DataTable: add `toolbarPrefix` slot rendered before the built-in search bar, for context filters (cluster / tenant selectors) that scope the whole list. Existing `toolbarLeft` (after search) is unchanged.
