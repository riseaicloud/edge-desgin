import { Label, Input } from '@riseaicloud/ui'
import { Preview } from '../../../components/Preview'

# Label

An accessible form label that pairs with form controls, built on Radix UI Label.

## Import

```tsx
import { Label } from '@riseaicloud/ui'
```

## Usage

<Preview center={false}>
  <div className="flex flex-col gap-1.5 w-64">
    <Label htmlFor="email-preview">Email address</Label>
    <Input id="email-preview" type="email" placeholder="you@example.com" />
  </div>
</Preview>

```tsx
import { Input } from '@riseaicloud/ui'

<div className="flex flex-col gap-1.5">
  <Label htmlFor="email">Email address</Label>
  <Input id="email" type="email" placeholder="you@example.com" />
</div>
```

## With required indicator

<Preview center={false}>
  <div className="flex flex-col gap-1.5 w-64">
    <Label htmlFor="name-preview">
      Name <span className="text-destructive">*</span>
    </Label>
    <Input id="name-preview" placeholder="Your name" />
  </div>
</Preview>

```tsx
<Label htmlFor="name">
  Name <span className="text-destructive">*</span>
</Label>
```

## Disabled state

When the associated control is disabled, the label is automatically styled as disabled:

<Preview center={false}>
  <div className="flex flex-col gap-1.5 w-64">
    <Label htmlFor="disabled-preview">Disabled Field</Label>
    <Input id="disabled-preview" disabled />
  </div>
</Preview>

```tsx
<div className="flex flex-col gap-1.5">
  <Label htmlFor="disabled-field">Disabled Field</Label>
  <Input id="disabled-field" disabled />
</div>
```

## API

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `htmlFor` | `string` | — | Associates the label with a form control by id |
| `className` | `string` | — | Additional CSS classes |

All standard HTML `label` attributes are also supported. The label inherits `peer-disabled` styles to match the disabled state of its paired control.
