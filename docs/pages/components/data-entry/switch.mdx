import { Switch, Label } from '@riseaicloud/ui'
import { Preview } from '../../../components/Preview'

# Switch

A toggle switch for binary on/off states.

## Import

```tsx
import { Switch } from '@riseaicloud/ui'
```

## Usage

<Preview>
  <Switch />
  <Switch defaultChecked />
</Preview>

```tsx
<Switch />
```

## With label

<Preview>
  <div className="flex items-center gap-2">
    <Switch id="notifications-preview" />
    <Label htmlFor="notifications-preview">Enable notifications</Label>
  </div>
</Preview>

```tsx
import { Label } from '@riseaicloud/ui'

<div className="flex items-center gap-2">
  <Switch id="notifications" />
  <Label htmlFor="notifications">Enable notifications</Label>
</div>
```

## Controlled

```tsx
import { useState } from 'react'

function ControlledSwitch() {
  const [enabled, setEnabled] = useState(false)
  return (
    <Switch
      checked={enabled}
      onCheckedChange={setEnabled}
    />
  )
}
```

## Disabled

<Preview>
  <Switch disabled />
  <Switch disabled defaultChecked />
</Preview>

```tsx
<Switch disabled />
<Switch disabled checked />
```

## API

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `checked` | `boolean` | `false` | Controlled checked state |
| `onCheckedChange` | `(checked: boolean) => void` | — | Callback when checked state changes |
| `disabled` | `boolean` | `false` | Disabled state |
| `className` | `string` | — | Additional CSS classes |
